Finds the mode of an array.

###Usage

```ruby
Mode.mode([1, 1, 2, 3]) == [1]
Mode.mode([1, 1, 2, 2, 3]) == [1, 2]
Mode.mode([1, 2, 3]) == [1, 2, 3]
```

###Tests

```bash
bundle install
bundle exec guard
```
